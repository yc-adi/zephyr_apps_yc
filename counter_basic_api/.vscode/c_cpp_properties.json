{
    "configurations": [
        {
            "name": "Win32",
            "includePath": [
                "${default}",
                "${workspaceFolder/../../../zephyr/include"
            ],
            "defines": [
                "${default}"
            ],
            "intelliSenseMode": "gcc-arm",
            "compilerPath": "${config:MAXIM_PATH}/Tools/GNUTools/10.3/bin/arm-none-eabi-gcc.exe",
            "browse": {
                "path": [
                    "${default}"
                ]
            }
        },
        {
            "name": "Linux",
            "includePath": [
                "${default}"
            ],
            "defines": [
                "${default}"
            ],
            "intelliSenseMode": "gcc-arm",
            "compilerPath": "${config:ARM_GCC_path}/bin/arm-none-eabi-gcc",
            "browse": {
                "path": [
                    "${default}"
                ]
            }
        },
        {
            "name": "Mac",
            "includePath": [
                "${default}"
            ],
            "defines": [
                "${default}"
            ],
            "intelliSenseMode": "gcc-arm",
            "compilerPath": "${config:ARM_GCC_path}/bin/arm-none-eabi-gcc",
            "browse": {
                "path": [
                    "${default}"
                ]
            }
        }
    ],
    "version": 4
}